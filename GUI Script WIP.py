#Import toolkit interface as our GUI:
from tkinter import *
#Create interface and give it a title:
window = Tk()
window.title('Progressive Pi Calculator')

#Dot-Suffix "mainloop" to the window, to capture window modifications (such as when the window is closed):
window.mainloop()
